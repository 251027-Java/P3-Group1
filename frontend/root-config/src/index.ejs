<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>GameHub - Micro-Frontend Application</title>
  <meta name="description" content="GameHub - Full-stack game hosting platform">
  <!-- Import map for MFE module resolution -->
  <% if (isLocal) { %>
  <script type="systemjs-importmap">
    {
      "imports": {
        "@<%= orgName %>/root-config": "//localhost:9000/gamehub-root-config.js",
        "@<%= orgName %>/react-users": "//localhost:3000/gamehub-react-users.js",
        "@<%= orgName %>/angular-auth": "//localhost:4200/main.js"
      }
    }
  </script>
  <% } else { %>
  <script type="systemjs-importmap">
    {
      "imports": {
        "@<%= orgName %>/root-config": "/gamehub-root-config.js",
        "@<%= orgName %>/react-users": "//react-frontend:3000/gamehub-react-users.js",
        "@<%= orgName %>/angular-auth": "//angular-frontend:4200/main.js"
      }
    }
  </script>
  <% } %>
  <!-- Shared dependencies import map -->
  <script type="systemjs-importmap">
    {
      "imports": {
        "single-spa": "https://cdn.jsdelivr.net/npm/single-spa@6.0.1/lib/system/single-spa.min.js",
        "react": "https://cdn.jsdelivr.net/npm/react@19.0.0/umd/react.production.min.js",
        "react-dom": "https://cdn.jsdelivr.net/npm/react-dom@19.0.0/umd/react-dom.production.min.js",
        "react-dom/client": "https://cdn.jsdelivr.net/npm/react-dom@19.0.0/umd/react-dom.production.min.js"
      }
    }
  </script>
  <!-- SystemJS core -->
  <script src="https://cdn.jsdelivr.net/npm/systemjs@6.14.3/dist/system.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/systemjs@6.14.3/dist/extras/amd.min.js"></script>
  <!-- Import map overrides for development -->
  <script src="https://cdn.jsdelivr.net/npm/import-map-overrides@4.0.0/dist/import-map-overrides.js"></script>
  <% if (isLocal) { %>
  <script src="https://cdn.jsdelivr.net/npm/import-map-overrides@4.0.0/dist/import-map-overrides.js"></script>
  <% } %>
  <style>
    /* Global styles for all MFEs */
    * {
      box-sizing: border-box;
    }
    body {
      margin: 0;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
      background-color: #0f0f23;
      color: #ffffff;
    }
    #single-spa-application\\:@gamehub\\/react-users,
    #single-spa-application\\:@gamehub\\/angular-auth {
      min-height: 100vh;
    }
    /* Loading indicator */
    .loading-container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      background: linear-gradient(135deg, #1a1a2e 0%, #16213e 100%);
    }
    .loading-spinner {
      width: 50px;
      height: 50px;
      border: 3px solid #333;
      border-top-color: #6366f1;
      border-radius: 50%;
      animation: spin 1s linear infinite;
    }
    @keyframes spin {
      to { transform: rotate(360deg); }
    }
  </style>
</head>
<body>
  <noscript>
    You need to enable JavaScript to run this application.
  </noscript>
  
  <div id="loading" class="loading-container">
    <div class="loading-spinner"></div>
  </div>

  <script>
    // Hide loading indicator after apps load
    System.import('@<%= orgName %>/root-config').then(() => {
      document.getElementById('loading').style.display = 'none';
    });
  </script>
  
  <!-- Import map overrides UI for development -->
  <% if (isLocal) { %>
  <import-map-overrides-full show-when-local-storage="devtools" dev-libs></import-map-overrides-full>
  <% } %>
</body>
</html>
